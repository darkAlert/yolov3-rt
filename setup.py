import setuptools

setuptools.setup(
    name='yolort',
    version='1.0.0',
    description='YOLO-RT: Real-time YOLOv3 in PyTorch',
    author='ultralytics,darkAlert',
    packages=setuptools.find_packages()
)
